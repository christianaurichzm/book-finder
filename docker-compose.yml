version: '3.1'
services:
  postgresql:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=bookfinder
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
